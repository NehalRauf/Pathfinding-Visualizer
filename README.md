# Pathfinding-Visualizer
Interactive desktop visualizer that runs A*, Greedy Best-First, and UCS side by side on the same grid maze. Draw walls, set start/end points, and watch each algorithm explore in real time with a live table comparing nodes expanded, path length, and runtime. Built in pure Python with Tkinter.
